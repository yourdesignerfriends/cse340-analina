<h1><%= title %></h1>

<% if (locals.message) { %>
    <div class="notice"><%= message %></div>
<% } %>

<% if (errors) { %>
  <ul class="notice">
    <% errors.array().forEach(error => { %>
        <li><%= error.msg %></li>
    <% }) %>
  </ul>
<% } %>

<%- editForm %>